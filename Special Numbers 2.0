n = int(input())
for num in range(1, n + 1):
    digit_sum = 0
    for digit in str(num):
        digit_sum += int(digit)
    if digit_sum in [5, 7, 11]:
        print(f"{num} -> True")
    else:
        print(f"{num} -> False")
